const conf = {
    authUrl: String(import.meta.env.VITE_AUTH_URL),
    chatUrl: String(import.meta.env.VITE_CHAT_URL),
    messagesUrl: String(import.meta.env.VITE_MESSAGES_URL),
    backendUrl: String(import.meta.env.VITE_BACKEND_URL),
}

export default conf